import { AdminAction } from "./state";

export default function adminReducer(state, action: AdminAction) {
  switch (action.type) {
    case 'getUsers': {
      return 
    }
  
    default:
      break;
  }
}